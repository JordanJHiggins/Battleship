import Ship from './ship';

const GameBoard = () => {
  const boardSize = 10;
  let missedAttack = 0;

  const createBoard = () => {};
  const placeShip = () => {};
  const receiveAttack = () => {};
  const fleetSunk = () => {};

  return { placeShip, receiveAttack, fleetSunk };
};

export default GameBoard;
